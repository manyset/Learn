<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <system.webServer>

        <!-- CRITICAL: Headers for OPFS -->
        <httpProtocol>
            <customHeaders>
                <add name="Cross-Origin-Opener-Policy" value="same-origin" />
                <add name="Cross-Origin-Embedder-Policy" value="require-corp" />
            </customHeaders>
        </httpProtocol>

        <!-- CRITICAL: MIME types for SQLite files -->
        <staticContent>
            <remove fileExtension=".wasm" />
            <remove fileExtension=".mjs" />
            <mimeMap fileExtension=".wasm" mimeType="application/wasm" />
            <mimeMap fileExtension=".mjs" mimeType="application/javascript" />
        </staticContent>

        <!-- Disable caching during development -->
        <caching>
            <profiles>
                <add extension=".html" policy="DisableCache" />
                <add extension=".js" policy="DisableCache" />
                <add extension=".mjs" policy="DisableCache" />
                <add extension=".wasm" policy="DisableCache" />
            </profiles>
        </caching>
        <defaultDocument>
            <files>
                <clear />
                <add value="index.html" />
                <add value="Default.htm" />
                <add value="Default.asp" />
                <add value="index.htm" />
                <add value="iisstart.htm" />
            </files>
        </defaultDocument>
    </system.webServer>
</configuration>